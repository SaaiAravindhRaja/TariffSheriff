# Test Configuration
# Inherits from main application.properties and overrides specific settings

# Use test database (can be same as dev for integration tests)
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://ep-broad-bar-a14utknk-pooler.ap-southeast-1.aws.neon.tech:5432/tariffsheriff?sslmode=require}
spring.datasource.username=${DATABASE_USERNAME:app_dev}
spring.datasource.password=${DATABASE_PASSWORD:2vKk?uw3mhdF}

# Disable Flyway for tests (assume schema already exists)
spring.flyway.enabled=false

# JPA settings for tests
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false

# OpenAI Configuration (use environment variable)
openai.api-key=${OPENAI_API_KEY}
openai.model=${OPENAI_MODEL:gpt-4o-mini}
openai.base-url=${OPENAI_BASE_URL:https://api.openai.com/v1}
openai.max-tokens=${OPENAI_MAX_TOKENS:1000}
openai.temperature=${OPENAI_TEMPERATURE:0.7}
openai.timeout-ms=${OPENAI_TIMEOUT_MS:30000}
openai.max-retries=${OPENAI_MAX_RETRIES:2}

# Chatbot Tool Configuration
chatbot.tools.default-timeout-ms=10000
chatbot.tools.max-concurrent-executions=5
chatbot.tools.enable-health-checks=false

# Enable all tools for testing
chatbot.tools.tools.getTariffRateLookup.enabled=true
chatbot.tools.tools.findHsCodeForProduct.enabled=true
chatbot.tools.tools.getAgreementsByCountry.enabled=true

# Logging
logging.level.com.tariffsheriff.backend.chatbot=DEBUG
logging.level.org.springframework.web.reactive.function.client=DEBUG
