{
  "name": "tariff-sheriff",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/frontend",
    "packages/*"
  ],
  "scripts": {
    "install:all": "npm install",
    "build": "npm run build --workspace=frontend",
    "test": "npm run test --workspaces"
  },
  "dependencies": {
    "jwt-decode": "^4.0.0"
  }
}
