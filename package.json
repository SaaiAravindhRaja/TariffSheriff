{
  "name": "tariff-sheriff",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/frontend",
    "packages/*"
  ],
  "scripts": {
    "install:all": "npm install",
    "build": "npm run build --workspace=frontend",
    "test": "npm run test --workspaces"
  },
  "overrides": {
    "js-yaml": "4.1.1",
    "vite": {
      "esbuild": "0.25.10"
    }
  }
}
